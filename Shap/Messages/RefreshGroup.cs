namespace Shap.Messages
{
    /// <summary>
    /// Message class, used to indicate that the group configuration list has changed and anything
    /// which uses it should reload the list.
    /// </summary>
    public class RefreshGroup
    {
    }
}