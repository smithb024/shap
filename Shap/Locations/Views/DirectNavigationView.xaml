<UserControl x:Class="Shap.Locations.Views.DirectNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shap.Locations.Views"
             mc:Ignorable="d" 
           d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ComboBox SelectedIndex="{Binding Path=SearchCriteriaIndex}"
                  Grid.Column="1"
                  ItemsSource="{Binding Path=SearchCriteria}"
                  Style="{StaticResource ComboBoxStyle}"/>
    </Grid>
</UserControl>
